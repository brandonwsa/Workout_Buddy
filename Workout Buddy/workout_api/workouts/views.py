from django.shortcuts import render
from .models import Workouts

"""
def workouts(request):
    content = {
        'workouts' : Workouts.obects.all()
    }

    return render(request, 'main/home.html', content)

"""